# Contributing to Hey

Hey 是一个网络隧道与协议调试客户端。**服务器由用户自己提供，应用不提供、不推荐、不引导任何服务器。**

## 设计原则

> **不主观提供任何用途，允许用户自行摸索。**

提交代码前，对照这一条问一句：

**这个东西是应用替用户做的决定，还是用户自己做的决定？**

- 用户自己做的决定 → 保留，并且要让它好用。支持一种协议、解析一种配置格式、
  暴露一个传输参数，都属于这一类，不受限制。
- 应用替用户做的决定 → 不要加。预设规则、内置源、自动选择、按地区分流的硬编码逻辑，
  都属于这一类。

这条判据区分的是**主张用途**和**提供能力**，不是砍功能。解析某种文本格式不等于引导服务商；
支持某个协议不等于推荐某种用法。

## 永不实现

以下内容不接受任何形式的 PR，包括"可选开关"和"默认关闭"：

1. **服务商专有响应头**：`subscription-userinfo`、`profile-update-interval`、
   `profile-web-page-url` 等的解析或展示（流量额度、到期时间、面板地址）。
   这类响应头除服务商面板外没有任何服务器会下发，实现即坐实产品定位。
2. **任何以国家或地区命名的预设**，或内容为国家 / 地区 IP 段与域名集合的
   预置规则、预置 DNS、预置数据文件源。
3. **任何内置的第三方规则集下载地址**。数据文件的地址一律由用户自行填写。
4. **任何内置的服务器地址、配置源地址、服务商引导链接**。
5. **任何伪装成其它客户端的 User-Agent**。应用只声明自身标识；
   用户可以在单条配置源上自行覆盖。
6. **任何把凭证做成可传播物的能力**：配置源 URL 的二维码、系统分享投递。
   用户导出自己的配置不在此列——那是用户的事。
7. **任何未经用户逐次确认就添加配置或发起网络请求的外部入口**（深链、分享、扫码）。
8. **任何按地理位置或服务商归属做的自动决策**：自动选择应用、自动选择线路、按国家分流。
9. **出口地址的地理归属解析与展示**。只回报出口 IP。

## 默认值规则

出厂默认值不得指向任何具体服务商或探测端点：

- 延迟测试地址：默认空。空时退化为到服务器的 TCP 握手时延。
- 出口地址查询：默认空。空时不发起请求。
- DNS：默认空，跟随系统。
- 传输参数（fragment 等）：保留参数入口，但不预调数值。

## 提交前

```bash
./scripts/compliance_scan.sh
```

命中即失败。词表见脚本内注释；`LICENSE` 与 `THIRD-PARTY-NOTICES.md` 已排除——
它们是 GPL-3.0 法定原文与上游署名，逐字随附是许可证义务。

跑单元测试：

```bash
node "$HVIGORW" --mode module -p module=entry@default -p isLocalTest=true test --no-daemon
```

## Commit message

一句话英文，描述做了什么，不描述为什么。
