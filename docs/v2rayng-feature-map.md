# v2rayNG Feature Parity Map

This project targets feature parity with the Android reference v2rayNG
(<https://github.com/2dust/v2rayNG>) without copying Android/Kotlin source,
XML layouts, drawable assets, launcher artwork, strings, or package branding.

Status below is maintained against the actual code; routing, subscription, and
custom config import status were refreshed on 2026-06-20. For the phased
delivery plan, see
[`development-plan.md`](development-plan.md). For an in-depth analysis of
v2rayNG's features and design, see
[`v2rayng-analysis/`](v2rayng-analysis/README.md).

## Page Map

| Android reference area | Harmony page | Current status |
| --- | --- | --- |
| MainActivity / MainRecyclerAdapter | Nodes | Present. Lists subscription nodes, search, select current node, start/stop/restart VPN, and honors the double-column display setting. Runtime-affecting changes made from settings, routing, per-app proxy, import/scan/manual add, current-node selection, or current custom-node editing set a v2rayNG-style restart marker that the home page consumes to auto stop/start the running service. |
| Add config menu | Add | Present. Imports share links, outbound JSON, proxy-chain JSON, policy-group JSON, or subscription URL; proxy-chain/policy-group configs can also be built from existing ordinary outbound nodes through the advanced outbound member selector and reopened from node detail for v2rayNG-style edit-in-place. Policy groups can use static selected members or v2rayNG-style dynamic members from a subscription group plus case-insensitive node-name regex filter. Protocol tabs are scaffolded. |
| ScannerActivity / ScScannerActivity | Scan | Present. Paste/import path and camera QR capture via ScanKit are wired; clipboard reads are attempted without declaring restricted Harmony `READ_PASTEBOARD`, so ordinary debug installs are not blocked and read failures fall back to the current localized error prompts; scanner clipboard success/empty/error prompts follow the current app language; scanner import now reuses the native share conversion fallback after single-link parsing fails, so v2rayN multi-line/base64 and Clash.Meta YAML text/QR payloads can be imported like the Add page; `startScanImmediate` matches v2rayNG by controlling whether the scanner opens the camera automatically when the page starts. |
| SubSettingActivity / SubEditActivity | Subs | Multi-group model present. Save/update/update-all/select/enable-disable/delete/reorder/share are wired; dedicated edit form supports name, URL, User-Agent, node-name filter, subscription-level insecure URL opt-in, auto-update opt-in, and update interval. Subscription URL clipboard paste prompts follow the current app language. Subscription URLs can be shared through Harmony `sendData` with clipboard fallback, and the detail page can render a QR code for the subscription URL. Foreground due refresh is wired, and Harmony WorkScheduler background refresh is code-complete pending real-device wakeup regression. |
| ServerActivity protocol editors | Add | Partial. Field-level editor is present for VLESS/VMess/Trojan/Shadowsocks/SOCKS/HTTP/WireGuard/Hysteria2 and saves validated outbound JSON; manually added ordinary nodes can be reopened from node detail, prefilled into the same editor, and updated in place like v2rayNG. Supported Xray transport options include `tcp/kcp/ws/httpupgrade/xhttp/h2/grpc`; TCP `none/http` header camouflage, KCP `headerType/seed/mtu/tti`, H2 comma-separated host lists, gRPC `gun/multi`, Hysteria2 `mport/mportHopInt`/`pinSHA256`/bandwidth, XHTTP `mode/extra`, and transport finalMask raw JSON can be written and refilled. XHTTP mode is constrained to v2rayNG `auto/packet-up/stream-up/stream-one` and invalid imported/manual/runtime values fall back to `auto`. Hysteria2 bandwidth/obfs/port-hop are lowered into v2rayNG-style runtime `finalmask.quicParams`/`udpHop`/`salamander`. VMess security options mirror v2rayNG `securitys`, VLESS flow options include v2rayNG's `xtls-rprx-vision` and `xtls-rprx-vision-udp443`, Shadowsocks method options mirror v2rayNG `ss_securitys`, TLS/Reality uTLS fingerprints include `chrome/firefox/safari/ios/android/edge/360/qq/random/randomized`, and TLS/Reality ALPN options mirror v2rayNG `streamsecurity_alpn`. Reality fields include `spiderX` and `mldsa65Verify`/`pqv` for VLESS/Trojan. TUIC is not targeted for Xray-core runtime because v2rayNG keeps it disabled and current Xray-core config docs do not expose a TUIC outbound. |
| ServerCustomConfigActivity | Add | Present for pasted/file JSON. Raw outbound JSON and full Xray config import work; full configs persist as custom manual nodes and start without Hey wrapping them in generated TUN/routing config, except for v2rayNG-compatible `routing.rules.process` UID normalization when process routing is available and package names can be resolved. Manual full custom config nodes can be reopened from node detail, edited with a name plus JSON content, revalidated, and saved back to the same node/profile when selected. |
| RoutingSettingActivity / RoutingEditActivity | Route | Present for core ruleset management. Traffic mode (global/rules/direct), v2rayNG domain strategy values (`AsIs`, `IPIfNonMatch`, `IPOnDemand`), bypass-LAN/CN, ad-block, custom routing rules, locked rules, predefined ruleset import, clipboard import, and clipboard export persist and feed generated Xray routing. Process matchers are stored as package names and converted to Harmony app UID strings only when v2rayNG `canUseProcessRouting` would allow them (`routeOnly` on and Hev TUN off), with `__unknown_app__` mapped to `-1`. Proxy-chain and policy-group runtime generation are available through JSON-imported or UI-built advanced nodes; UI-built advanced nodes can be edited in place; routing-rule target selection can use the current advanced outbound. |
| SettingsActivity | Config | Core, VPN DNS, IPv6 enable, IPv6 preference, local HTTP proxy append/sharing, local SOCKS proxy static/dynamic port/UDP/auth with v2rayNG-style default-on `pref_enable_local_proxy` and default port 10808, Hev TUN preferences (`pref_use_hev_tunnel_v2`, `pref_hev_tunnel_loglevel`, `pref_hev_tunnel_rw_timeout_v2`) with v2rayNG defaults and local SOCKS force-on parity while Harmony still runs Xray TUN, mux, sniffing, log level, speed display, current connection info test URL, language auto-follow, UI mode, pre-release update check, all-groups display, double-column display, real-ping concurrency, delete confirmation, immediate scan startup, auto connect at startup, outbound domain pre-resolve method, and routing strategy persist; language maps v2rayNG `auto/en/zh` to Harmony system language resolution or explicit English/Chinese, UI mode maps v2rayNG `0/1/2` to Harmony follow-system/light/dark color mode, pre-release update check shares `pref_check_update_pre_release` with About's GitHub release picker, all-groups display adds an All tab that aggregates nodes from every subscription group for search, delay testing, and export, double-column display switches the Nodes list between one and two lanes, the IP API URL defaults to v2rayNG's `https://api.ip.sb/geoip` and is queried through the local HTTP proxy after a successful start, VPN DNS filters to pure IPs for Harmony `VpnConfig.dnsAddresses` and empty/invalid values fall back to v2rayNG `AppConfig.DNS_VPN` (`1.1.1.1`), VPN MTU defaults to v2rayNG's 1500 and feeds both Harmony VPN and Xray TUN config, `ipv6Enabled` feeds the VPN interface IPv6 address/route and WireGuard IPv6 local address retention, `preferIpv6` feeds generated Xray outbound Happy Eyeballs, proxy sharing changes local HTTP/SOCKS inbound listen, dynamic SOCKS port resolves a free runtime port before start, remote/domestic DNS filter to v2rayNG pure IP/CoreDNS address forms before feeding generated Xray DNS with v2rayNG-style domain-bound server selection and DNS routing, fake DNS and DNS hosts accept v2rayNG settings, and `pref_outbound_domain_resolve_method=0/1/2` resolves outbound domains through Harmony system DNS before startup, merges live results with user DNS hosts, and then either writes outbound `UseIP`/`happyEyeballs` or replaces matching outbound domains. Mux/XUDP/fragment advanced fields feed outbound runtime settings, with XUDP UDP/443 policy constrained to v2rayNG's `reject/allow/skip` and Fragment packets constrained to v2rayNG's `tlshello/1-2/1-3/1-5`; core loglevel is constrained to v2rayNG's `debug/info/warning/error/none` across Settings, storage, and runtime generation; speed display controls generated metrics/stats/policy, runtime traffic display, and an ongoing Harmony speed notification refreshed every 3 seconds while connected, real-ping concurrency controls batch delay testing, delete confirmation controls node/subscription removal dialogs, immediate scan startup controls Scanner page auto-launch, and auto connect at startup handles Harmony `AUTO_STARTUP` launches when the system/user permits app auto-start. Remaining gaps are deeper platform-specific settings and full device regression. |
| PerAppProxyActivity | Apps | Partial. Toggle, allowlist/blocklist mode, preset/manual package list, select all/clear/invert/auto-proxy selection for the visible app list, v2rayNG-style clipboard import/export of `bypass + package list`, default proxy-selected mode, empty-list self blocking, and VPN `blockedApplications`/`trustedApplications` mapping are wired. Auto-proxy selection now fetches v2rayNG's `2dust/androidpackagenamelist` remote `proxy.txt`, retries through Hey's local HTTP proxy when available, and falls back to the embedded list. Unrestricted installed-app enumeration is platform-limited. |
| UserAssetActivity / UserAssetUrlActivity / BackupActivity | Assets | Present. geosite/geoip plus v2rayNG's forced `geoip-only-cn-private.dat` download, v2rayNG Geo source choices (`Loyalsoldier/v2ray-rules-dat`, `runetfreedom/russia-v2ray-rules-dat`, `Chocolate4U/Iran-v2ray-rules`), custom asset URL CRUD with v2rayNG-style duplicate-name rejection, local file assets hidden from edit like v2rayNG `url == "file"` rows, v2rayNG-style asset URL QR import with add-form prefill, clipboard backup/restore, WebDAV ZIP cloud backup/restore, and native Geo count status are implemented. |
| LogcatActivity | Logs | Present. App diagnostic logs and native runtime stats are visible; the page supports v2rayNG-style search filtering, copy-all, share-all through the Harmony share sheet with clipboard fallback, single-line tap copy, and clear-all. |
| AboutActivity | About | Present. Harmony-specific about/license note, native Xray core version display, and v2rayNG-style GitHub release update check that parses tag/assets, compares against the current app version, honors the pre-release check toggle, and opens the download/release page when a newer version exists. |
| TaskerActivity / shortcuts / widgets / QS tile | Platform equivalents | Partial. Harmony control deep links cover the core start/stop/toggle/scan actions (`hey://control?action=...` plus short hosts) and can be invoked by external Want/automation surfaces. A 2×2 ArkTS desktop service card is registered through `ControlCardAbility` and uses `FormLink` to call `hey://toggle`, `hey://start`, `hey://stop`, and `hey://scan`; form IDs and latest card state are persisted, and runtime status changes refresh the card through `formProvider.updateForm` with a 3-second throttle. True device add/click and launcher refresh regression remain pending. |
| UrlSchemeActivity | Import entry | Present. Harmony Want/deep-link import handles subscription and config links, including v2rayNG-style outer URI fragment fallback for `install-sub` / `install-config` names when the embedded URL has no fragment. Harmony `sendData` / `text/plain` external share intents are also registered and consumed like v2rayNG `ACTION_SEND`, so shared text can import subscription URLs, single share links, and native-converted batch/base64/Clash payloads. |

## Business Function Map

| Function | Current status |
| --- | --- |
| Subscription URL fetch and parse | Present for `vless://`, `vmess://`, `trojan://`, `ss://`, `socks://`, `socks4://`, `socks5://`, `http://`, `https://`, `wireguard://`, `hysteria2://`, `hy2://`. |
| Node select and save current profile | Present. |
| Xray config generation | Present for outbound + native TUN inbound or v2rayNG-style `Proxy only` local SOCKS runtime + optional speed metrics inbound + optional local HTTP/SOCKS proxy inbounds/listen sharing + dynamic local SOCKS runtime port + proxy-chain multi-hop outbounds via `sockopt.dialerProxy` + policy-group `routing.balancers`/observatory generation + basic direct/block outbounds. The local SOCKS inbound follows v2rayNG `pref_enable_local_proxy` by defaulting on, using default port 10808, and disappearing only when the user disables it; the Harmony HTTP proxy substitute follows the same local-proxy switch and is cleared/ignored when local proxy is disabled. DNS hosts and live Harmony system DNS pre-resolution can drive v2rayNG-style outbound domain pre-resolve modes by adding `UseIP`/`happyEyeballs` or replacing matching outbound domains before Xray starts; generated DNS hosts also include v2rayNG's `googleapis.cn` and Android Private DNS default mappings while preserving user override order. Remote/domestic DNS generation now filters upstreams to v2rayNG pure IP/CoreDNS forms with default fallback and mirrors v2rayNG domain-bound server selection for proxy/direct rules, CN `expectIPs`, DNS module proxy/direct routing, block hosts, and parallel query when multiple upstreams exist. FakeDNS sniffing mirrors v2rayNG by adding sniffing `fakedns` whenever FakeDNS is enabled, while top-level `fakedns` and DNS FakeDNS server still require local DNS plus FakeDNS. TLS/Reality runtime generation now mirrors v2rayNG by filling missing `serverName` from transport host/authority or the server domain and disabling `allowInsecure` when `pinnedPeerCertSha256` is configured. Hysteria2 runtime generation now mirrors v2rayNG by converting share/manual `hysteria2` nodes into Xray core `protocol=hysteria` outbounds with `settings.address/port/version`, `streamSettings.hysteriaSettings.auth`, TLS/pin settings, v2rayNG default ALPN `h3`, and finalmask. Global Mux follows v2rayNG by skipping Shadowsocks/SOCKS/HTTP/Trojan/WireGuard/Hysteria2/Hysteria and XHTTP outbounds, preserving `-1..1024` Mux/XUDP concurrency values including `-1/0`, disabling any imported existing mux when global mux is off or the target protocol is skipped, and using `concurrency=-1` for VLESS flow nodes. Global Fragment follows v2rayNG by generating TLS/Reality `streamSettings.finalmask.tcp/udp`, adjusting Reality `tlshello` to `1-3`, forcing TLS packets to `tlshello`, and skipping existing finalmask or `dialerProxy` chain hops. The v2rayNG routeOnly setting now feeds TUN sniffing `routeOnly`, including the disabled-sniffing edge case where v2rayNG still keeps `enabled=false` with `routeOnly=true`, and gates custom routing `process` matchers so process-only rules are omitted unless routeOnly is enabled and Hev TUN is disabled; enabled process matchers are converted from package names to Harmony app UID strings, with unresolved ordinary packages dropped and `__unknown_app__` mapped to `-1`. Generated routing rule IP matchers rewrite `geoip:cn/private` to `ext:geoip-only-cn-private.dat:cn/private` like v2rayNG while DNS `expectIPs` keep `geoip:cn`. Existing ordinary outbound nodes can be converted into proxy-chain/policy-group JSON through Add > advanced config; policy groups can resolve members dynamically from the latest subscription group and case-insensitive node-name regex filter at startup; full custom Xray configs are validated and passed through at runtime with v2rayNG-compatible `routing.rules.process` UID replacement only when process routing is available and a package resolves. |
| Persistent app settings | Present for core VPN, v2rayNG `pref_mode` (`VPN` / `Proxy only`), DNS, DNS hosts, outbound domain pre-resolve method, IPv6 enable/prefer split, mux/XUDP/fragment, sniffing, routeOnly, default-on local proxy, Hev TUN preference storage/defaults, log, speed display, current connection info test URL, language `auto/en/zh`, pre-release update checks exposed in Settings and About, UI mode, all-groups display, double-column display, real-ping concurrency, delete confirmation, immediate scan startup, auto connect at startup, and routing strategy; selected values are applied at app launch/foreground, save time, connection start, update check time, batch test time, Scanner startup, destructive actions, node list rendering, Harmony auto-start launch handling, or runtime notification sync, including language resolution through Harmony system language, Harmony color mode, all-groups node aggregation, double-column node list lanes, IP API lookup via the generated local HTTP proxy, VPN interface DNS pure-IP filtering, generated Xray DNS upstream filtering/hosts, VPN IPv6 routing, WireGuard IPv6 local address filtering, outbound Happy Eyeballs, outbound domain pre-resolve via static DNS hosts plus startup live DNS results, mux settings, v2rayNG-constrained outbound fragment packet settings, v2rayNG routing domain strategy normalization, routeOnly sniffing and v2rayNG-gated process UID routing, default local SOCKS generation, local proxy switch gating of append-HTTP proxy, Hev-on local SOCKS force-on parity (platform-limited; Harmony runtime still uses Xray TUN), speed metrics generation/display/ongoing notification, delay-test concurrency, scan auto-launch, deletion confirmation, startup auto-connect when system auto-start is granted, and native proxy-only start/stop without VPN authorization. Like v2rayNG `SettingsChangeManager`, runtime-affecting changes mark the running service for restart and the home page consumes that marker on return, deferring if a connection operation is busy. |
| VPN Extension start/stop | Present, with emulator timeout diagnostics. Real-device verified for IPv4 data path (2026-06-15); IPv6 interface address/route generation is wired and awaits real-device regression. |
| Xray native TUN runtime | Present in HAP via `CGoSetTunFd` + Xray TUN inbound. Real-device closed loop verified (2026-06-15): TUN → Xray → outbound → reachable. |
| VMess/VLESS/Trojan/Shadowsocks parsing | Present. |
| SOCKS/HTTP/WireGuard/Hysteria2 parsing | Present for share-link import, subscription discovery, manual outbound editing, and manual-node edit refill; WireGuard `[Interface]/[Peer]` `.conf` text/file import is also supported. VLESS flow, uTLS `fp`, VMess QR `insecure` imports and exports v2rayNG-style `1/0`, VMess URL-style user security follows v2rayNG default `auto` while `security=tls/reality` is parsed as stream security, URL-style TLS `insecure`/`allowInsecure` export aliases, URL-style userInfo URI encoding, empty URL fragment `none` default names, finalMask `fm`, TCP `headerType=http/none`, VLESS/Trojan bare outbound export default TCP `security/type/headerType` query, KCP `headerType/seed/mtu/tti`, gRPC `mode=multi`, SOCKS v2rayNG-style no-padding `base64(user:pass)` userInfo plus no-auth `Og` export and `socks4://` / `socks5://` import aliases, Shadowsocks legacy base64 URLs with optional trailing slash and colon-containing passwords, Shadowsocks/SOCKS no-padding base64 userInfo export, Shadowsocks SIP002 `obfs=http` plugin import with v2rayNG-style no-plugin export, WireGuard reserved default `0,0,0` and MTU default `1420`, Hysteria2 `security=tls` plus `insecure=1/0` export, `mportHopInt` and `pinSHA256`, HTTPUpgrade host/path, XHTTP `mode/extra` with v2rayNG mode enum fallback, TLS/Reality `ech`/`pcs`, and Reality `spx`/`pqv` parameters round-trip through share-link import/export; VLESS flow, uTLS `fp`, finalMask raw JSON, TCP HTTP header camouflage, KCP, gRPC `gun/multi`, HTTPUpgrade, XHTTP `mode/extra`, Hysteria2 `mportHopInt`/`pinSHA256`/bandwidth, and TLS/Reality `ech`/`pcs` can be filled or refilled from NodeEdit manual forms, and TLS insecure aliases (`insecure`/`allowInsecure`/`allow_insecure`) are accepted on import. Hysteria2 bandwidth/obfs/port-hop now feed runtime finalmask generation and HY2 runtime outbounds are normalized to Xray `hysteria`; TLS/Reality runtime output fills missing SNI like v2rayNG, and TLS pinned-certificate runtime output suppresses `allowInsecure`. Runtime connection for WireGuard and real HY2 still needs device/core validation. |
| TUIC parsing | Not targeted for the current Xray-core runtime; v2rayNG's TUIC enum is commented out and the official Xray-core config documentation does not expose a TUIC outbound. |
| Delay test / real ping / sort by delay | Present. The Nodes menu exposes TCP endpoint delay, v2rayNG-style real config delay, and a manual v2rayNG `sort_by_test_results` equivalent. Real delay uses libXray `CGoPing` with its own SOCKS test inbound, prefers `CGoGetFreePorts` dynamic ports with 10825 fallback, strips outbound mux from the speedtest config like v2rayNG, honors the saved `delayTestUrl` target, persists results per node, and can auto sort/remove by result. Batch delay testing honors the v2rayNG-style concurrency setting (default 16, clamped to 1..128), and manual/auto sorting plus auto-remove follows v2rayNG's subscription grouping semantics when the All virtual group is selected. Falls back to direct URL test when the native core is unavailable (e.g. emulator). Needs real-device validation. |
| Delete all / duplicate / invalid configs | Present. Delete-all, duplicate cleanup, test-failed invalid-node cleanup, single-node deletion, and v2rayNG-style optional per-config deletion confirmation are wired from the Nodes menu/list actions. Duplicate cleanup now follows v2rayNG profile comparison semantics for ordinary nodes by ignoring remarks/raw-link fragment and runtime tag differences, while skipping full custom/proxy-chain/policy-group nodes. Batch delete/dedupe/invalid cleanup follows the currently visible node scope, including the All virtual group across subscription groups. |
| Export/share configs and QR generation | Present for text-oriented flows. Plain-text share-link/exported JSON output is present, clipboard import reads are attempted without declaring restricted Harmony pasteboard permissions so ordinary debug installs are not blocked, batch export follows v2rayNG `shareNonCustomConfigsToClipboard` semantics by including only shareable ordinary nodes, skipping full custom/proxy-chain/policy-group/invalid configs, and counting actual exported rows; node detail can copy v2rayNG-style full generated Xray config content for ordinary, full custom, proxy-chain, and policy-group nodes; node detail can render QR codes for share-link nodes, subscription detail can render QR codes for subscription URLs, Export can save the current group to a `.txt` file, and batch/single-node/subscription text can be sent through the Harmony system share sheet with clipboard fallback. |
| Multi-subscription groups | Present with legacy single-subscription migration. Rename/edit, enable-disable, delete, delete confirmation, reorder, batch update all, subscription URL QR/text sharing, per-subscription insecure URL opt-in, auto-update opt-in, and update interval are wired. Foreground due refresh is wired, subscription fetches can prefer the local HTTP proxy when the VPN runtime exposes it, and WorkScheduler background refresh is code-complete pending real-device wakeup regression. |
| Routing rulesets and geo assets | Partial. Geo asset download/management present (Assets page), including the v2rayNG `geoip-only-cn-private.dat` built-in file used by ext GeoIP routing and the v2rayNG Loyalsoldier/Russia/Iran Geo source choices, with custom asset URLs addable manually, from local `.dat`, or from QR-scanned URL like v2rayNG, duplicate asset names rejected like v2rayNG remarks, local imported assets refreshable/deletable but not editable like v2rayNG file rows, and native geosite/geoip count sidecars shown in file status through the rebuilt `libxray.so`. Clipboard backup/restore and WebDAV ZIP cloud backup/restore are wired, with legacy JSON backup restore compatibility. Routing config emits speed metrics when enabled, ad-block, custom enabled rules, process UID matchers, and bypass-LAN/CN rules in order. Predefined ruleset import/export is wired, including v2rayNG China whitelist/blacklist public DNS IP/domain entries, and custom rulesets can be imported from clipboard or QR code JSON like v2rayNG while preserving locked rules; proxy-chain and policy-group runtime nodes are supported through JSON import and the advanced outbound builder, routing rules can target the current advanced outbound, and policy-group subscription-regex dynamic members resolve at startup with case-insensitive node-name filtering. |
| Per-app proxy | Partial. VPN app allow/block mapping is wired from saved package names, with visible-list batch selection, auto-proxy selection from the v2rayNG remote package list plus embedded fallback, clipboard import/export, v2rayNG default proxy-selected mode, and empty-list self blocking; unrestricted installed-app enumeration remains platform-limited. |
| Auto subscription update | Partial. Per-subscription `autoUpdate` and `updateIntervalMinutes` persist with v2rayNG-compatible defaults (1440 minutes, minimum 15); the home page performs throttled foreground due refresh through the local HTTP proxy when available. Harmony WorkScheduler now registers a persisted repeating background task at the shortest enabled interval and runs due refresh from `SubscriptionUpdateWorkAbility`; real-device wakeup regression remains pending. |
| Boot/startup automation | Partial and platform-dependent. `startOnBoot` mirrors v2rayNG `pref_is_booted`; when Harmony launches the app with `AbilityConstant.LaunchReason.AUTO_STARTUP`, Hey consumes the pending startup marker after settings/nodes load and starts the selected node if the setting is enabled. The manifest does not declare restricted `ohos.permission.RECEIVER_STARTUP_COMPLETED`, avoiding ordinary debug-signature install failures while leaving startup behavior to Harmony's system auto-start launch path. Requires user/system auto-start enablement and real-device reboot regression. |

## Native Bridge Status

The packaged `libxray.so` exports 12 CGo functions; `napi_init.cpp` wires all 12 (M1, refreshed 2026-06-19).

- Wired (runtime): `CGoRunXrayFromJSON`, `CGoStopXray`, `CGoPing`, `CGoSetTunFd`.
- Wired (M1): `CGoQueryStats` (real per-tag traffic via the Xray metrics
  `/debug/vars` endpoint), `CGoTestXray` (pre-connect config preflight),
  `CGoXrayVersion` (core version on the About page),
  `CGoReadGeoFiles` / `CGoCountGeoData` (Geo file validation/count status on Assets),
  `CGoGetFreePorts` (dynamic ports for real outbound delay tests and local SOCKS runtime port),
  `CGoConvertShareLinksToXrayJson` / `CGOConvertXrayJsonToShareLinks`
  (native share text ⇄ Xray JSON conversion).
- The legacy upstream `CGoRunXray` entry is not a Hey runtime target; Hey starts Xray with `CGoRunXrayFromJSON`.

> M1 note: the prebuilt `libxray.so` version script previously exported only the
> 4 runtime symbols (`local: *` hid the rest). `scripts/build_libxray_ohos.sh`
> now also exports `CGoQueryStats`/`CGoTestXray`/`CGoXrayVersion` plus the Geo
> count/read symbols, `CGoGetFreePorts`, and the share conversion symbols, and
> the packaged `libxray.so` was rebuilt on 2026-06-19. `llvm-nm -D` confirms all
> 12 CGo symbols are exported and wired. Device retest is still required for the
> optional paths.
